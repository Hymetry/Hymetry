build:
  docker:
    web: Dockerfile

run:
  web:
    image: web
    command:
      - gunicorn config.wsgi:application --bind 0.0.0.0:$PORT

  worker:
    image: web
    command:
      - celery -A config worker --loglevel=info --concurrency=1 --max-tasks-per-child=50

  beat:
    image: web
    command:
      - celery -A config beat --loglevel=info