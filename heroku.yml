build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - sh -c "python manage.py migrate --noinput || sleep 600"
run:
  web: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
  worker: celery -A config worker --loglevel=info
  beat: celery -A config beat --loglevel=info